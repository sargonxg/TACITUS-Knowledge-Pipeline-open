# TACITUS Knowledge Pipeline — Environment Configuration
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ─── Google Cloud / Vertex AI ───────────────────────────────
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
VERTEX_AUTH_METHOD=colab          # colab | service_account_json

# ─── LLM Models ─────────────────────────────────────────────
GEMINI_MODEL=gemini-1.5-pro
EMBEDDING_MODEL=text-embedding-004

# ─── Neo4j ──────────────────────────────────────────────────
# Free tier: https://neo4j.com/cloud/aura-free/
NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-password
NEO4J_DATABASE=neo4j

# ─── FalkorDB (Optional) ────────────────────────────────────
# FALKORDB_HOST=localhost
# FALKORDB_PORT=6379
# FALKORDB_USERNAME=
# FALKORDB_PASSWORD=

# ─── Pipeline Behavior ──────────────────────────────────────
# TACITUS_CHUNK_SIZE=20000        # Characters per chunk
# TACITUS_CHUNK_OVERLAP=1000      # Overlap between chunks
# TACITUS_ER_THRESHOLD=85         # Entity resolution similarity (0-100)
# TACITUS_DEFAULT_DATE=2024-01-01
# TACITUS_DRY_RUN=false           # Set true to skip DB writes
